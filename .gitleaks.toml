# Gitleaks Configuration
# See: https://github.com/gitleaks/gitleaks#configuration

title = "Silo Gitleaks Config"

# Allowlist - Files and patterns that are known safe
[allowlist]
description = "Allowlisted files and patterns"

# Test scripts with intentionally invalid/fake tokens
paths = [
    '''scripts/verify_oidc\.sh''',
]
